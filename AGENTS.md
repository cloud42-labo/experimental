# AGENTS.md — Codexレビュー規約

このファイルは、ChatGPTの Codex（GitHub連携の Automatic reviews / `@codex review`）が
PRレビュー時に読む規約。人間・Claude Code向けの規約は [CLAUDE.md](CLAUDE.md) を参照
（内容はおおむね重なる）。

## このリポジトリについて

試作・検証用リポジトリ。**1ディレクトリ1プログラム**（ルート直下に、試したもの単位で
ディレクトリを切る。言語・フレームワークはディレクトリごとに異なってよい）。
本番運用や継続的なメンテナンスは前提にしない。詳細は [CLAUDE.md](CLAUDE.md)。

## Code Review Rules

レビューでは、一般的なコーディング規約の指摘だけでなく、**このリポジトリ固有で実際に
起きた失敗**を優先して探すこと（一般論のレビューはClaude自身の自己レビューと相関しやすく、
価値が低い）。

### 優先してチェックする項目

- **構成違反: 1つのPRが複数の試作ディレクトリにまたがっていないか。**
  「1ディレクトリ1プログラム」を破る変更（他ディレクトリのファイルを参照する、
  ルート直下に共通の設定ファイルを置く等）は指摘する
- **GitHub Actions の `if:` に `secrets.X` を直接書くと、ワークフロー全体が無効になる**
  （jobが1件も起きずに`failure`になる。GitHubの式検証の制約）。job の `env` に一度写して
  `env.X` 経由で参照すること
- **`run: |` の複数行文字列は、字下げがブロックより浅い行が来た時点で終わる。**
  文字列の途中でも関係なく終わる。複数行の本文（リリースノート等）はファイルに書き出して
  `--notes-file` / `--body-file` で渡すこと
- **依存ライブラリのバージョンを記憶で書かない。** 日付付きバージョン
  （`v4-revYYYYMMDD-2.0.0` 等）は、実在する日付か確認されていないと存在しないバージョンを
  指したままビルドが壊れる。レジストリで実在を確認したものか、PRの説明にその根拠があるかを見る
- **バージョニング規約に沿っているか。** アプリを変更したら該当する `APP_VERSION` /
  `versionName` 等のバージョン定数を更新しているか
- **署名鍵・APIキー・トークンなどの秘密情報がコード・ログ出力・コミットに混入していないか**
- **権限（GitHub Actions permissions 等）が変更の目的に対して過剰に広がっていないか**

### AIレビューでは検知できないもの（見つけたら明示的に指摘する）

コードを読むだけで、実機やCIでのビルド・実行はしない。UIの見た目・実機でのみ再現する
不具合（アイコン表示、位置情報、通知など）が絡む変更は、レビューが「問題なし」でも
実機不具合を否定できない。該当する変更を見つけたら「実機・ブラウザでの確認が必要」と明記する。

## 関連

- [CLAUDE.md](CLAUDE.md)
- [README.md](README.md) — PRレビュー・マージの全体像
- [cloud42-labo/brain](https://github.com/cloud42-labo/brain) の `decisions/` `notes/`
  （このリポジトリ・関連アプリで実際に起きた失敗の詳しい記録）
